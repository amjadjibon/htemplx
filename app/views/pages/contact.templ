package pages

import (
    "htemplx/app/views/components"
    "htemplx/app/views/layouts"
)

templ Contact(title string) {
    @layouts.Base(title) {
        @components.Navbar()
        @components.Contact()
        @components.Footer()
    }
}
